{
  "name": "trama-docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/copiar-templates.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/copiar-templates.mjs",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check",
    "pretest": "node scripts/copiar-templates.mjs",
    "test": "node --test --experimental-strip-types src/lib/*.test.ts"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "pagefind": "^1.5.2",
    "@astrojs/markdown-remark": "^7.3.1",
    "@types/node": "^26.6.2",
    "typescript": "^6.0.3"
  }
}
