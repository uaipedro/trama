% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scheduler.R
\name{tr_scheduler}
\alias{tr_scheduler}
\title{Scheduler: a execução como máquina de passos.}
\usage{
tr_scheduler(
  plan,
  registry = .tr_default_registry,
  store,
  executor = tr_executor_sequential(),
  on_event = function(ev) invisible(NULL),
  run_id = NULL,
  inherit = list(),
  ctx_extra = NULL
)
}
\description{
\code{tr_run_plan()} era um \code{while} com \code{Sys.sleep}, e \code{tr_server} o chamava
dentro de um \code{observeEvent} — o processo do Shiny ficava travado até o run
acabar, MESMO com o pool \code{mirai}. Três promessas do design dependiam de o
coordenador ficar livre e não existiam: cancelar unidade superada, mostrar
progresso enquanto roda, mostrar parcial. Aqui o estado é explícito e
\code{step()} faz um passo (despacha o que está pronto, coleta o que terminou).
Headless: \code{while (!s$step()) ...}. Shiny: \code{later::later(step)}.
}
\details{
DOIS conjuntos, e a distinção importa:
\code{done}    — terminou com valor bom (executou agora, ou veio do cache).
\code{skipped} — não vai produzir valor (falhou, inválido, bloqueado).

A versão anterior inferia "terminou" de "não está mais em \code{pending}" — e
uma unidade EM VOO também não está em \code{pending}. Com capacidade > 1 o
consumidor era despachado antes do produtor gravar, falhava com "chave
ausente", e — pior — esse erro virava handle no store: como a chave é
determinística, o erro falso ficava cacheado com a mesma autoridade de um
resultado bom, para sempre.
}
