% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type.R
\name{tr_type}
\alias{tr_type}
\title{Declara um tipo de dado (o que trafega numa porta).}
\usage{
tr_type(
  id,
  version = 1L,
  label = NULL,
  color = "#64748b",
  store = NULL,
  restore = NULL,
  ext = "rds",
  preview = NULL,
  summary = NULL
)
}
\description{
\strong{O tipo carrega a própria persistência} (\code{store}/\code{restore}/\code{ext}). É aqui
que "SpatRaster é ponteiro C++ e não sobrevive a saveRDS" se resolve — não
no store, que permanece cego ao que guarda.
}
\details{
O insumo despachava serialização por classe R (\code{inherits(x, "SpatRaster")}),
e isso não escala nem dentro de um domínio só: \code{Heightmap} e \code{Field} são
ambos \code{SpatRaster}, e foi exatamente essa colisão que obrigou a existência
de um nó identidade (\code{height_as_field}) só pra reetiquetar um no outro.
Tipo é nominal; classe R é detalhe de implementação de um tipo.

\code{preview(x, ctx)} devolve um artefato — \code{tr_preview()} — cujo \code{renderer} é
um id de string resolvido no front. Dispatch por id, nunca por \code{inherits()}:
é o que permite uma coleção trazer visualização nova sem tocar no núcleo.

Sem \code{store}/\code{restore}, cai em RDS — suficiente pra tipo que é objeto R
comum (data.frame, lista, escalar).
}
\examples{
# Sem `store`/`restore` o artefato cai em RDS, que basta para um escalar.
num <- tr_type("exemplo/num", label = "Numero", color = "#818cf8",
               preview = function(x, ctx) {
                 tr_preview("trama/keyvalue", data = list(valor = x))
               },
               summary = function(x) list(valor = x))
num$summary(7)

reg <- tr_registry()
tr_use("trama", registry = reg)
tr_get_type("demo/num", reg)$label
}
