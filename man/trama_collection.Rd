% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo.R
\name{trama_collection}
\alias{trama_collection}
\title{A coleção \code{demo}, que o próprio núcleo traz.}
\usage{
trama_collection()
}
\value{
Um \code{tr_collection} com os tipos \code{demo/num} e \code{demo/tabela} e os
blocos \code{demo/const}, \code{demo/soma}, \code{demo/tabela}, \code{demo/filtrar} e
\code{demo/resumo}.
}
\description{
O núcleo do trama não tem domínio: bloco nenhum mora aqui, e é isso que
permite uma coleção nova chegar sem tocar no pacote. O preço é que o pacote
instalado sozinho não faz NADA — sem coleção não há bloco, não há fluxo, e
não há um único exemplo executável na ajuda. Quem instala fica olhando um
editor vazio, e quem revisa uma submissão vê 85 páginas de ajuda sem
exemplo.
}
\details{
A \code{demo} fecha esse buraco sem furar a regra: ela é uma coleção como
qualquer outra — registrada por \code{tr_use()}, com ids qualificados por
\verb{demo/} — e o que ela traz é aritmética e uma tabela de brinquedo, não
domínio. O núcleo continua cego ao que ela significa.

Ela é encontrada pelo mesmo caminho de toda coleção: \code{tr_use()} recebe o
NOME DE UM PACOTE e chama a função \code{trama_collection()} do namespace dele.
Como quem exporta aqui é o próprio \code{trama}, o nome do pacote é \code{"trama"} e o
id da coleção é \code{demo} — os dois só coincidem por acidente nas outras
coleções (\code{trama.data} traz a coleção \code{data}), então nenhum caso especial
entra em \code{tr_use()}.

Tudo em R base, de propósito: uma coleção de demonstração que exigisse
dplyr ou ggplot2 transformaria o primeiro contato com o pacote numa
instalação de dependências.
}
\examples{
# A coleção que acompanha o pacote, num registro próprio.
reg <- tr_registry()
tr_use("trama", registry = reg)
names(reg$nodes)

# Um fluxo se escreve em R e vira o mesmo documento que a interface produz.
f <- tr_flow(reg) |>
  tr_add("dois", "demo/const", value = 2) |>
  tr_add("tres", "demo/const", value = 3) |>
  tr_add("soma", "demo/soma", from = c("dois", "tres"), k = 10)
cat(tr_flow_code(f, reg), sep = "\n")

# E roda, com um store numa pasta temporária.
s <- tr_store(file.path(tempdir(), "demo-store"))
tr_value(tr_flow_doc(f), "soma", reg, s)
}
