% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{tr_theme}
\alias{tr_theme}
\title{Resolve um tema pelo nome, fora de um projeto.}
\usage{
tr_theme(tema = "padrão", settings = NULL)
}
\arguments{
\item{tema}{Nome de um tema, \code{"padrão"}, ou a definição já resolvida (lista).}

\item{settings}{Settings do projeto; \code{NULL} usa os temas embutidos.}
}
\value{
Lista com \code{nome}, os campos do tema e, se o nome não existe,
\code{ausente = TRUE}.
}
\description{
Para o console e para coleções: \code{fn} de nó recebe a definição já resolvida
pelo plano, mas quem chama a função direto passa um nome ("claro"). Sem
projeto, valem os temas embutidos.
}
\details{
Aceitar também a lista já resolvida é o que deixa a coleção ter UM caminho
só: ela chama \code{tr_theme()} sem saber de onde veio o valor. A lista é
revalidada porque no console qualquer um monta uma à mão, e \code{fudno} calado
seria o mesmo silêncio que o manifesto recusa. Nome desconhecido não é erro
aqui, pelo mesmo motivo do plano (documento não quebra por cosmético): volta
o padrão marcado \code{ausente}, e quem chama decide se isso é erro.
}
