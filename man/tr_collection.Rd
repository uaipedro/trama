% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{tr_collection}
\alias{tr_collection}
\title{Agrupa tipos, nós, adaptadores e categorias de um domínio.}
\usage{
tr_collection(
  id,
  version = "0.0.0",
  label = id,
  types = list(),
  nodes = list(),
  adapters = list(),
  categories = list(),
  js = NULL,
  css = NULL,
  migrations = list()
)
}
\arguments{
\item{js}{Caminho, relativo ao \verb{inst/} do pacote, do módulo ES da coleção.
Opcional. Os arquivos vizinhos no mesmo diretório também são servidos, pro
módulo poder importá-los por caminho relativo.}

\item{css}{Caminho, relativo ao \verb{inst/} do pacote, de uma folha de estilo da
coleção (as classes que os renderers dela usam). Opcional. Entra na página
DEPOIS do \code{trama.css} do núcleo, então pode refinar regra do núcleo com a
mesma especificidade — e é por isso mesmo que deve prefixar as próprias
classes, pra não refinar sem querer.}

\item{migrations}{Renomes que a coleção declara pra documentos antigos
abrirem já migrados, sem nó órfão. Lista com até três entradas, todas
opcionais:
\itemize{
\item \code{nodes}: id antigo -> id novo (\code{list("multi/roc" = "models/roc")}). O id
antigo pode ser de OUTRA coleção — é assim que um bloco muda de casa —,
mas o destino tem que ser desta.
\item \code{params}: id NOVO do nó -> lista de renomes \code{antigo = list(to = "novo", value = function(v) ...)}. \code{value} é opcional (identidade).
\item \code{ports}: id NOVO do nó -> lista \code{antiga = "nova"}, valendo pra entradas e
saídas.
}

Aplicadas por \code{tr_doc_migrate()} quando um fluxo é aberto.}
}
\description{
Uma coleção é o que um pacote de domínio exporta. \code{js} aponta pra um módulo
ES em \verb{inst/} que registra renderers e widgets no front — é o que permite
visualização nova sem tocar no núcleo.
}
\details{
\code{label} é o nome que a coleção mostra na aba da paleta; sem ele, a aba
escreve o \code{id}, que serve mas não explica.
}
