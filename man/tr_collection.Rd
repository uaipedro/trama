% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{tr_collection}
\alias{tr_collection}
\title{Agrupa tipos, nós, adaptadores e categorias de um domínio.}
\usage{
tr_collection(
  id,
  version = "0.0.0",
  label = id,
  types = list(),
  nodes = list(),
  adapters = list(),
  categories = list(),
  js = NULL,
  css = NULL,
  migrations = list()
)
}
\arguments{
\item{js}{Caminho, relativo ao \verb{inst/} do pacote, do módulo ES da coleção.
Opcional. Os arquivos vizinhos no mesmo diretório também são servidos, pro
módulo poder importá-los por caminho relativo.}

\item{css}{Caminho, relativo ao \verb{inst/} do pacote, de uma folha de estilo da
coleção (as classes que os renderers dela usam). Opcional. Entra na página
DEPOIS do \code{trama.css} do núcleo, então pode refinar regra do núcleo com a
mesma especificidade — e é por isso mesmo que deve prefixar as próprias
classes, pra não refinar sem querer.}

\item{migrations}{Renomes que a coleção declara pra documentos antigos
abrirem já migrados, sem nó órfão. Lista com até três entradas, todas
opcionais:
\itemize{
\item \code{nodes}: id antigo -> id novo (\code{list("multi/roc" = "models/roc")}). O id
antigo pode ser de OUTRA coleção — é assim que um bloco muda de casa —,
mas o destino tem que ser desta. Quando o nó novo precisa de um param
pra se comportar como o antigo, o destino pode ser
\code{list(to = "novo", params = list(nome = valor))}: os params entram SÓ
nos nós cujo tipo era o id antigo, antes dos renomes de \code{params}, e não
sobrescrevem param já presente. Em cadeia (a -> b -> c) os params de
cada salto se acumulam na ordem. Ex.:
\code{"multi/logistic_coefficients" = list(to = "models/coefficients", params = list(exponenciar = TRUE))}.
\item \code{params}: id NOVO do nó -> lista de renomes \code{antigo = list(to = "novo", value = function(v) ..., when = function(v) ...)}. \code{value} é opcional
(identidade); \code{when} é um predicado sobre o valor antigo, e \code{value} só
roda quando ele é verdadeiro. Dois casos especiais:
\itemize{
\item converter no lugar: \code{to} igual ao nome antigo. Aí \code{when} é
obrigatório — é ele que reconhece o valor ainda no formato velho e
deixa intocado o documento já migrado:
\code{confianca = list(to = "confianca", when = is.character, value = function(v) as.numeric(sub("\%", "", v)) / 100)}.
\item derivar vários params: \code{value} devolve lista NOMEADA, fundida aos
params do nó (param já presente com esse nome vence):
\code{intervalo = list(to = "intervalo", when = function(v) grepl("^IC ", v), value = function(v) list(intervalo = "IC", confianca = 0.9))}.
}
\item \code{ports}: id NOVO do nó -> lista \code{antiga = "nova"}, valendo pra entradas e
saídas.
}

Aplicadas por \code{tr_doc_migrate()} quando um fluxo é aberto.}
}
\description{
Uma coleção é o que um pacote de domínio exporta. \code{js} aponta pra um módulo
ES em \verb{inst/} que registra renderers e widgets no front — é o que permite
visualização nova sem tocar no núcleo.
}
\details{
\code{label} é o nome que a coleção mostra na aba da paleta; sem ele, a aba
escreve o \code{id}, que serve mas não explica.
}
