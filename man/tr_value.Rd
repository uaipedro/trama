% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{tr_value}
\alias{tr_value}
\title{Valor de qualquer nó do grafo, no console.}
\usage{
tr_value(
  doc,
  node,
  registry = .tr_default_registry,
  store,
  port = NULL,
  executor = tr_executor_sequential(),
  settings = NULL,
  ctx_extra = NULL
)
}
\description{
O caminho inverso do nível 1 (\code{tr_fn(id)}), e a outra metade do que impede
a ferramenta de virar gaiola: dá pra inspecionar no REPL o que qualquer
caixa da tela produziu, sem passar pela UI.
}
\details{
Reaproveita o plano que \code{tr_run()} já devolve, em vez de montar outro. Um
segundo plano sorteia um \code{nonce} novo pra nó \code{volatile}, e a chave
consultada não seria a que acabou de ser gravada.

\code{settings} é o \code{project$settings}: tema entra no hash, e sem ele o console
resolveria o tema pelos embutidos e calcularia chave diferente da sessão —
recomputando o que já está no cache, ou pior, mostrando outro gráfico.

\code{ctx_extra} está aqui porque pedir o valor de um nó no console RODA o grafo, e
uma região de dez mil pontos é justamente o que alguém pede assim: sem o
argumento, o único jeito de baixar a cadência de checkpoint num run de console
seria não usar \code{tr_value()}.
}
