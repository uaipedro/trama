% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migrate.R
\name{tr_doc_migrate}
\alias{tr_doc_migrate}
\title{Leva um documento salvo ao formato do registro atual.}
\usage{
tr_doc_migrate(doc, registry = .tr_default_registry)
}
\description{
Um só ponto de entrada, dois mecanismos, sempre nesta ordem:
}
\details{
\enumerate{
\item \strong{Migrações de coleção} (\code{tr_collection(migrations = )}): renomes de
tipo de nó (inclusive entre coleções, em cadeia), de param (com
conversão de valor) e de porta. Dependem só do NOME antigo, não da
versão.
\item \strong{Migrações por versão do nó} (\code{tr_node(migracoes = )}): funções por
versão de destino, aplicadas da \code{type_version} gravada até a da spec.
}

Nó cujo TIPO foi renomeado no passo 1 não passa pelo passo 2: a
\code{type_version} gravada é a do nó antigo (outro tipo, outra numeração), e
aplicá-la ao destino rodaria funções erradas. O renome declarado é quem
leva os params ao formato atual do destino; o nó sai com a \code{type_version}
da spec de destino. Nó não renomeado passa pelos dois: o renome de param
da coleção vem antes, e as funções por versão tratam param ausente como
já migrado.

Idempotente. O resultado leva \code{attr(, "migrated") = TRUE} quando algo
mudou (o editor regrava no autosave) e \code{attr(, "migracoes")} com o
registro das subidas de versão (o editor e \code{tr_project_flow()} avisam).
}
