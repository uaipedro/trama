% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{tr_retry}
\alias{tr_retry}
\title{Comando: limpa SÓ o(s) handle(s) de erro de um nó, preservando o checkpoint.}
\usage{
tr_retry(doc, node, registry = .tr_default_registry, store, settings = NULL)
}
\value{
\code{invisible(TRUE)} se limpou pelo menos um handle de erro;
\code{invisible(FALSE)} se não havia nada a limpar (nó sem handle).
}
\description{
O gesto explícito que faltou depois da Fase 5. Uma falha grava handle de
ERRO sob toda porta de saída da unidade (\code{R/scheduler.R}, o ramo de falha de
\code{collect()}, via \code{tr_store_put_error}); \code{tr_plan()} marca a unidade
\code{failed}, e o scheduler a serve direto de \code{skipped}/\code{from_cache} —
\code{tr_run()} sozinho, no MESMO store, nunca redespacha, mesmo com um
\code{ckpt.rds} bom esperando no disco (\code{R/stream-driver.R}: falha no \code{step},
falha no colapso e worker morto guardam o checkpoint DE PROPÓSITO). O único
jeito público de tirar o handle de erro era \code{tr_bust()} — que apaga
\verb{<store>/stream/} inteiro (\code{R/hash.R}), porque bustar significa "o código
mudou sem a chave mudar", e aí retomar serviria um histórico metade velho,
metade novo. Usado contra um erro comum, \code{tr_bust()} jogaria fora os pontos
bons do checkpoint pela mesma chamada que limpa o erro do último ponto —
exatamente o custo que a retenção do checkpoint existe pra evitar.
}
\details{
\code{tr_retry()} faz só a metade que falta: tira o(s) handle(s) de erro da
unidade e NUNCA toca \verb{<store>/stream/<chave da unidade>/}. Não é
\code{tr_bust()} com um filtro a mais — é o oposto dele: um preserva o
checkpoint de propósito, o outro apaga de propósito, e os dois precisam
continuar existindo porque servem casos diferentes ("o ponto 10.000 errou,
quero retomar dali" vs. "mudei o código, o checkpoint velho não presta
mais").

Recusa apagar um handle BOM (\code{tr_handle_failed()} falso): um artefato
válido não é candidato a retry — apagá-lo seria um \code{tr_bust()} disfarçado,
silencioso, sem o comentário que explica a perda do checkpoint. A recusa é
TRANSACIONAL: se qualquer porta de saída da unidade guarda um artefato bom,
a chamada aborta ANTES de apagar qualquer handle de erro das outras portas
— uma região de dois colapsos não pode sair com um retry parcial.

Nó sem handle nenhum (nunca rodou, ou já foi limpo) é NO-OP silencioso: a
mesma forma que \code{tr_stream_command()} escolheu pra chave sem diretório
(\code{R/stream-control.R}) — clique perdido não é erro.

Membro INTERIOR de uma região não tem unidade nem chave de saída própria
(ver o comentário de \code{tr_value()} acima, e \code{plan$region_of}): pedir retry
dele significa "retry a região", resolvida pelo mesmo mapa que \code{tr_value()}
já usa pra leitura — o efeito observável é o de pedir retry por qualquer um
dos colapsos dela.

\code{settings} existe pelo mesmo motivo que em \code{tr_value()}: sem ele o plano
calcularia a chave com o tema embutido, e talvez não achasse a MESMA
unidade (nem as MESMAS chaves de saída) que o run de verdade usou.

Não roda nada — só planeja, pra achar a unidade e as chaves, e mexe no
store. É COMANDO, não op de documento: não toca \code{doc}, \code{rev} nem o log de
undo (Decisão 10), do mesmo jeito que pause/step/stop de
\code{R/stream-control.R}.
}
