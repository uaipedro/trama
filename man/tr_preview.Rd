% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type.R
\name{tr_preview}
\alias{tr_preview}
\title{Artefato de preview: o que o worker grava e o front desenha.}
\usage{
tr_preview(renderer, data = NULL, files = NULL)
}
\description{
\code{renderer} é o id do componente JS que sabe desenhar isso. \code{data} viaja no
payload (dado já reduzido — downsample, bins, head); \code{files} são caminhos
relativos no store, pra quando o dado não cabe (imagem raster).
Regra: \strong{arquivo só quando o dado não cabe.}
}
\examples{
tr_preview("trama/keyvalue", data = list(valor = 7))

# O preview de um tipo registrado sai pronto para virar JSON.
reg <- tr_registry()
tr_use("trama", registry = reg)
tabela <- tr_fn("demo/tabela", reg)()
p <- tr_get_type("demo/tabela", reg)$preview(tabela, NULL)
p$renderer
p$data$columns
}
