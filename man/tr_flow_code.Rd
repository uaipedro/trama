% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow.R
\name{tr_flow_code}
\alias{tr_flow_code}
\title{Gera o código R (DSL) que reconstrói o documento. É a outra metade da
promessa "diagrama e código são a mesma coisa": o que se monta na interface
se lê como R, e o que se escreve em R aparece na interface.}
\usage{
tr_flow_code(doc, registry = .tr_default_registry, registry_expr = "reg")
}
\description{
Ordem topológica; params só quando diferem do default; \verb{from =} quando a
ligação é a canônica (primeira saída -> primeira entrada obrigatória livre),
senão \code{tr_link()} explícito. Posições NÃO entram: são apresentação.
}
\details{
Param cujo NOME é formal de \code{tr_add()} (\code{from}, \code{label}, \code{seed}, \code{position},
e também \code{type}, \code{id}, \code{flow}) não pode sair dentro do \code{tr_add()}: o R casa
o nome com o formal, não com o param — é a armadilha que
\code{.tr_check_param_shadow()} denuncia do lado de quem escreve. Emitido
ingenuamente, \code{data/rename} (param \code{from}) saía como
\code{tr_add("ren", "data/rename", from = "mpg", to = "consumo", from = "ler")}:
argumento duplicado, código que não roda. Esses params saem num \code{tr_set()}
encadeado — o caminho que o próprio guard manda usar — e, quando o param
sombreado é \code{from}, as arestas do nó saem por \code{tr_link()} explícito, porque
o formal \code{from} continua vedado para aquele \code{tr_add()}.
}
