% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runs.R
\name{tr_project_gc}
\alias{tr_project_gc}
\title{GC do projeto: preserva tudo que QUALQUER flow do projeto alcança. \code{tr_store_gc}
recebia \code{keep} de um documento só; com dois flows no mesmo projeto, coletar
pelo primeiro apagaria o cache do segundo. Falha de leitura de um flow não
impede o GC dos outros, mas aborta a coleta — melhor não coletar do que
apagar o que um flow ilegível alcançava. Os \code{settings} do projeto entram no
plano pelo mesmo motivo: sem eles a chave de quem usa tema do projeto seria
outra, e o cache vivo pareceria órfão.}
\usage{
tr_project_gc(project, max_age_days = 7)
}
\description{
GC do projeto: preserva tudo que QUALQUER flow do projeto alcança. \code{tr_store_gc}
recebia \code{keep} de um documento só; com dois flows no mesmo projeto, coletar
pelo primeiro apagaria o cache do segundo. Falha de leitura de um flow não
impede o GC dos outros, mas aborta a coleta — melhor não coletar do que
apagar o que um flow ilegível alcançava. Os \code{settings} do projeto entram no
plano pelo mesmo motivo: sem eles a chave de quem usa tema do projeto seria
outra, e o cache vivo pareceria órfão.
}
