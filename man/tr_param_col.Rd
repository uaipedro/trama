% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param.R
\name{tr_param_col}
\alias{tr_param_col}
\title{Param de coluna(s) da tabela que chega por uma entrada.}
\usage{
tr_param_col(
  default = "",
  label = NULL,
  role = "qualquer",
  multi = FALSE,
  from = NULL,
  example = NULL
)
}
\arguments{
\item{default}{Valor inicial (texto).}

\item{label}{Rótulo do campo.}

\item{role}{\code{"numerica"}, \code{"categorica"}, \code{"tempo"} ou \code{"qualquer"}.}

\item{multi}{\code{TRUE} aceita várias colunas separadas por vírgula.}

\item{from}{Nome da entrada cujas colunas servem; \code{NULL} é a primeira.}

\item{example}{Exemplo mostrado no campo.}
}
\description{
\code{role} diz que tipo de coluna serve; \code{multi = FALSE} é uma coluna só (select);
\code{from} nomeia a entrada (padrão: a primeira do nó). Continua \code{kind = "cols"}:
o valor é texto ("a" ou "a, b"), igual aos \code{cols} já existentes — quem já
declara \code{tr_param("cols", ...)} segue valendo, só sem as anotações que
deixam o front oferecer as colunas do \code{schema} da entrada.
}
\details{
\code{role} inválido aborta com \code{tr_error_bad_param} e não com o erro cru de
\code{match.arg}: é erro de declaração da coleção, da mesma família dos outros
que \code{tr_node()} levanta.
}
