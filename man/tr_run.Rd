% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{tr_run}
\alias{tr_run}
\title{Coordenador: monta o plano, despacha o que falta, emite eventos.}
\usage{
tr_run(
  doc,
  targets = NULL,
  registry = .tr_default_registry,
  store,
  executor = tr_executor_sequential(),
  on_event = function(ev) invisible(NULL),
  run_id = NULL,
  settings = NULL
)
}
\description{
Pull-based herdado do insumo — nó fora do caminho do alvo não roda. O
"acender os cards em sequência" continua saindo de graça da ordem real de
execução: o coordenador não calcula fila nenhuma, só emite o que acontece.
}
\details{
\code{on_event(ev)} recebe, por unidade: \code{cached}, \code{failed}, \code{invalid},
\code{blocked}, \code{running}, \code{done}; e um \code{run_finished} no fim. É o contrato que
o transporte (E5) traduz pra mensagem de front — o motor não sabe o que é
Shiny.
}
