% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icon.R
\name{tr_icon}
\alias{tr_icon}
\title{Declara o ícone de um bloco: um nome do conjunto Lucide, ou um SVG próprio.}
\usage{
tr_icon(name = NULL, svg = NULL)
}
\description{
Exatamente um dos dois. \code{name} é conferido contra os ícones que o sprite
realmente tem — sem isso, \code{tr_icon("filter")} (que não existe; o nome é
\code{list-filter}) viraria um buraco silencioso no canto do card, descoberto
meses depois. Mesma régua de \code{tr_node()} sem \code{description}: erro alto, na
declaração.
}
\details{
\code{svg} é o MIOLO de um \verb{<svg>}, não um documento: o elemento e o \code{viewBox}
de 24×24 são do front, a coleção entrega a geometria. Não é barreira de
confiança — uma coleção é um pacote R instalado e já roda JS próprio — é só
o que faz o ícone de terceiro herdar cor e tamanho como os outros.
}
