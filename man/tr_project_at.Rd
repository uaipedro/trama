% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{tr_project_at}
\alias{tr_project_at}
\title{Abre um projeto REUSANDO um registry já carregado.}
\usage{
tr_project_at(root, registry)
}
\description{
É o que permite trocar de projeto com o editor em pé. \code{tr_project()} monta um
registry novo, carregando cada coleção do manifesto — e é isso que não se pode
fazer numa página que já está aberta: o JS e o CSS de cada coleção entram como
\code{htmlDependency} quando a UI é montada (\code{tr_ui()}), uma vez só. Registry novo
com coleção nova no R e nenhum renderer no navegador seria um card verde sem
desenho nenhum.
}
\details{
Daí a recusa ser a regra, e não um remendo: se o manifesto pede coleção que a
página não tem, isto aborta NOMEANDO-A, antes de criar pasta ou tocar em
qualquer estado. É o diagnóstico para o qual o manifesto existe.

A comparação é contra o PACOTE (\verb{$package}), não contra o id da coleção: o
manifesto guarda \code{trama.data} e o registry indexa por \code{data}, e os dois só
coincidem por acidente (ver \code{tr_use()}).
}
