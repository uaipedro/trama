% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node.R
\name{tr_node}
\alias{tr_node}
\title{Declara um node type.}
\usage{
tr_node(
  id,
  fn,
  version = 1L,
  label = NULL,
  description,
  help = NULL,
  category = NULL,
  inputs = list(),
  outputs = list(),
  params = list(),
  pure = TRUE,
  fingerprint = NULL,
  volatile = FALSE,
  stochastic = FALSE,
  icon = NULL
)
}
\description{
\strong{Sem efeito colateral}: devolve uma especificação. Quem registra é
\code{tr_collection()} + \code{tr_use()}. No insumo, \code{wm_register_node()} escrevia no
registro E criava o wrapper de nível 1 em \code{globalenv()} no ato — o que
impedia o projeto de virar pacote e impedia dois registros isolados
coexistirem (todo teste compartilhava o mesmo estado global).
}
\details{
\code{fn} é uma função R comum, usável direto no console — o nível 1 da API. Os
argumentos permitidos são os nomes de \code{inputs}, os de \code{params}, e os dois
reservados \code{.seed} e \code{.ctx} (com ponto, pra nunca colidirem com um param
que por acaso se chame \code{seed}).

\code{description} é obrigatória e \code{help} é opcional: a primeira é a linha que
aparece na paleta e no tooltip do card, a segunda é a página que abre no
painel lateral. Obrigar a primeira é o que impede uma coleção de nascer
muda — e o catálogo é lido por máquina também, então a ausência custa
duas vezes.

\code{pure = FALSE} exige \code{fingerprint(params)}: o que do mundo externo entra na
chave de cache (mtime+tamanho de arquivo, ETag de URL). Sem isso, um nó que
lê um CSV serviria dado velho em silêncio quando o arquivo muda, porque
nada no hash teria mudado. \code{volatile = TRUE} nunca cacheia entre execuções.
}
