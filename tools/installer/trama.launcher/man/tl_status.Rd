% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{tl_status}
\alias{tl_status}
\title{Status da instalação local}
\usage{
tl_status(m = tl_manifest_fetch(), s = tl_state_read())
}
\arguments{
\item{m}{Manifesto (de \code{tl_manifest_fetch()}) ou \code{NULL} se offline.}

\item{s}{Estado local (de \code{tl_state_read()}).}
}
\value{
Lista com \code{release_instalada}, \code{release_disponivel},
\code{atualizar}, \code{r_instalado}, \code{r_exigido}, \code{troca_de_r}, \code{pacotes}
(data.frame nome/instalada/disponível) e \code{colecoes} (data.frame
nome/titulo/instalada/disponivel/requires).
}
\description{
O que a tela de início lê para desenhar versões, botão de atualizar,
coleções e o aviso de troca de linha do R. Nunca lança erro — mesmo sem
instalação e sem rede, devolve uma resposta coerente (a página tem que
conseguir se desenhar nesse estado).
}
