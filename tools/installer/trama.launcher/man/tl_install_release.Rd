% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{tl_install_release}
\alias{tl_install_release}
\title{Instala uma release numa biblioteca isolada}
\usage{
tl_install_release(m, colecoes = tl_state_read()$colecoes, progresso = message)
}
\arguments{
\item{m}{Manifesto de release (\code{tl_manifest_read()}/\code{tl_manifest_fetch()}).}

\item{colecoes}{Nomes de coleções a instalar junto com o núcleo.}

\item{progresso}{Função chamada com uma mensagem a cada pacote (\code{message()} por padrão).}
}
\value{
(Invisível) o novo estado gravado.
}
\description{
Instala a release descrita em \code{m} (mais as \code{colecoes} pedidas) numa
biblioteca isolada \code{tl_lib_dir(m$trama)}, só com pacotes binários.
}
\details{
Só grava o estado se TODOS os pacotes pedidos existirem na lib ao final;
se algum faltar, apaga a lib parcial e lança erro sem tocar no estado —
quem já tinha uma release funcionando continua com ela.
}
