% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{tr_ml_pr_curve}
\alias{tr_ml_pr_curve}
\title{Curva precisão-revocação para classificação binária}
\usage{
tr_ml_pr_curve(
  dados,
  resposta = "",
  probabilidade = "",
  positiva = "",
  aspecto = "16:9",
  tema = "padrão",
  titulo = "",
  rotulo_x = "",
  rotulo_y = "",
  legenda = "direita"
)
}
\arguments{
\item{dados}{Tabela com a classe observada e a probabilidade prevista
(a saída de \code{models/predict}, com as colunas \verb{prob_<classe>}).}

\item{resposta}{Nome da coluna observada.}

\item{probabilidade}{Coluna com a probabilidade da classe positiva.}

\item{positiva}{Classe tratada como positiva. Vazio deduz a classe do nome da
coluna (\verb{prob_<classe>}, como sai de \code{models/predict}); se o nome não
indicar uma classe observada, o bloco recusa
(\code{tr_ml_error_positive_required}) em vez de adivinhar.}

\item{aspecto, tema, titulo, rotulo_x, rotulo_y, legenda}{Acabamento do gráfico,
como nos demais gráficos da coleção.}
}
\value{
Objeto \code{ggplot}. Os dados do gráfico trazem \code{limiar}, \code{recall},
\code{precision}, a \code{ap}, a \code{area} (Davis & Goadrich) e a \code{prevalencia}.
}
\description{
Ordena as linhas pela probabilidade da classe positiva e traça a precisão
contra a revocação em cada corte (empates num só degrau). A precisão média
(AP) é a soma, nos cortes, do ganho de revocação vezes a precisão
(sem interpolação; Su, Yuan & Zhu 2015). A linha de referência é a
prevalência da classe positiva, a precisão de um classificador ao acaso
(Saito & Rehmsmeier 2015). A \code{area} é a área sob a curva com a interpolação
não linear de Davis & Goadrich (2006), integrada de forma contínua; é menor
que a AP quando há degraus, e a interpolação linear da ROC não vale aqui.
}
\examples{
d <- data.frame(y = c("sim", "nao", "sim", "nao", "sim"),
                prob_sim = c(.9, .8, .7, .6, .2))
tr_ml_pr_curve(d, "y", "prob_sim")
}
