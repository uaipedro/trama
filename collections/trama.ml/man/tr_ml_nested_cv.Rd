% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{tr_ml_nested_cv}
\alias{tr_ml_nested_cv}
\title{Validação cruzada aninhada}
\usage{
tr_ml_nested_cv(
  dados,
  alvo = "",
  cols = "",
  modelo = "cart",
  tarefa = "auto",
  metrica = "auto",
  tentativas = 10L,
  folds_externos = 5L,
  folds = 3L,
  amplitude = "conservadora",
  estrategia = "aleatoria",
  ordem = "",
  grupo = "",
  seed = 42L
)
}
\arguments{
\item{dados}{Tabela de treino com pelo menos duas linhas.}

\item{alvo}{Nome de uma coluna existente em \code{dados}.}

\item{cols}{Preditores numéricos separados por vírgula. Vazio usa todos os
numéricos, exceto \code{alvo}.}

\item{modelo}{Família com hiperparâmetros: \code{"cart"}, \code{"figs"}, \code{"forest"},
\code{"svm"} ou \code{"xgboost"}.}

\item{tarefa}{Uma de \code{"auto"}, \code{"regressao"} ou \code{"classificacao"}. \code{"auto"}
interpreta resposta numérica como regressão.}

\item{metrica}{Métrica compatível com a tarefa. \code{"auto"} usa \code{"rmse"} em
regressão e \code{"macro_f1"} em classificação.}

\item{tentativas}{Número inteiro positivo de configurações avaliadas.}

\item{folds_externos}{Partições externas, a partir de dois.}

\item{folds}{Partições internas de cada busca.}

\item{amplitude}{Limites \code{"conservadora"} ou \code{"ampla"} para a busca.}

\item{estrategia}{Como formar os folds: \code{"aleatoria"} (padrão; estratificada
pela classe), \code{"grupo"} (grupos inteiros de \code{grupo} por fold) ou
\code{"temporal"} (origem móvel com janela crescente: os instantes de \code{ordem}
formam \code{folds + 1} blocos contíguos e cada fold valida o bloco seguinte ao
treino).}

\item{ordem}{Coluna de tempo para \code{estrategia = "temporal"}.}

\item{grupo}{Coluna de grupo para \code{estrategia = "grupo"}. Com \code{cols} vazio,
\code{ordem} e \code{grupo} nunca entram como preditores.}

\item{seed}{Inteiro entre zero e 2147483647. Controla folds, configurações e
ajustes sem alterar o estado aleatório da sessão.}
}
\value{
Tibble com uma linha por fold externo (\code{fold}, \code{n_treino},
\code{n_validacao}, \code{tentativa}, \code{interna} = média dos folds internos do
vencedor, otimista; \code{externa} = métrica na validação externa) e uma linha
final \code{fold = "media"} com as médias; a estimativa honesta é \code{externa}.
}
\description{
Estima o desempenho de todo o procedimento de ajuste — busca de
hiperparâmetros incluída — sem reaproveitar as linhas que escolheram o
vencedor (Varma & Simon 2006). Cada fold externo roda um \code{\link[=tr_ml_tune]{tr_ml_tune()}}
completo só no seu treino e mede o vencedor na sua validação, que a busca
nunca viu.
}
\examples{
d <- tr_ml_example("iris_binaria")
tr_ml_nested_cv(d, alvo = "Species", tentativas = 3, folds_externos = 3, folds = 3)
}
