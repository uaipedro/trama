% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{tr_ml_roc}
\alias{tr_ml_roc}
\title{Curva ROC para classificação binária}
\usage{
tr_ml_roc(
  dados,
  alvo = "",
  probabilidade = "",
  positiva = "",
  confianca = 0.95,
  permitir_treino = FALSE,
  aspecto = "16:9",
  tema = "padrão",
  titulo = "",
  rotulo_x = "",
  rotulo_y = "",
  legenda = "direita"
)
}
\arguments{
\item{dados}{Tabela com a classe observada e sua probabilidade prevista.}

\item{alvo}{Nome da coluna observada.}

\item{probabilidade}{Coluna com a probabilidade da classe positiva.}

\item{positiva}{Classe tratada como positiva. Vazio deduz a classe do nome da
coluna de probabilidade (\verb{.prob_<classe>}, como sai de \code{\link[=tr_ml_predict]{tr_ml_predict()}});
se o nome não indicar uma classe observada, o bloco recusa
(\code{tr_ml_error_positive_required}) em vez de adivinhar.}

\item{confianca}{Nível do intervalo de confiança da AUC (DeLong et al.
1988), entre 0 e 1.}

\item{permitir_treino}{Se FALSE (padrão), recusa linhas marcadas como
treino pelo \code{ml/split} (\code{tr_ml_error_train_eval}); se TRUE, desenha,
avisa e põe a nota de otimismo na legenda e na coluna \code{nota}.}

\item{aspecto}{Proporção do gráfico: \code{"16:9"}, \code{"4:3"}, \code{"1:1"}, \code{"3:4"}
ou \code{"2:1"}.}

\item{tema}{Nome de um tema registrado no projeto.}

\item{titulo}{Título do gráfico. Vazio omite o título.}

\item{rotulo_x}{Rótulo do eixo X. Vazio mantém o rótulo gerado.}

\item{rotulo_y}{Rótulo do eixo Y. Vazio mantém o rótulo gerado.}

\item{legenda}{Posição \code{"direita"} ou \code{"abaixo"}. \code{"nenhuma"} a omite.}
}
\value{
Objeto \code{ggplot} da curva ROC. Os dados do gráfico trazem, por corte,
\code{limiar} (prevê positivo com P >= limiar), \code{fpr} e \code{tpr}, e repetidos a
\code{auc}, seu erro-padrão (\code{auc_ep}) e intervalo (\code{auc_inf}, \code{auc_sup}) de
DeLong, \code{auc_nota} (NA, ou o motivo quando o IC é indisponível: AUC = 0
ou 1 dá variância de DeLong zero e o IC sai NA), o corte de Youden (\code{youden_limiar}, \code{youden_j}) e a marca
\code{youden} na linha escolhida.
}
\description{
Curva ROC para classificação binária
}
