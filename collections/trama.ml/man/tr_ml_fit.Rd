% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{tr_ml_fit}
\alias{tr_ml_fit}
\title{Ajustar um modelo clássico de aprendizado de máquina}
\usage{
tr_ml_fit(
  dados,
  alvo = "",
  cols = "",
  modelo = "cart",
  tarefa = "auto",
  seed = 42L,
  max_depth = 3L,
  min_n = 5L,
  max_splits = 6L,
  trees = 200L,
  mtry = 0L,
  cost = 1,
  gamma = 0.1,
  kernel = "radial",
  nrounds = 100L,
  eta = 0.1,
  cp = 0,
  poda = "1ep",
  corte = 0.5
)
}
\arguments{
\item{dados}{Tabela de treino.}

\item{alvo}{Nome da coluna a prever.}

\item{cols}{Preditores separados por vírgula; em branco, usa todas as
colunas numéricas, exceto o alvo.}

\item{modelo}{Um de \code{"linear"}, \code{"cart"}, \code{"figs"}, \code{"forest"}, \code{"svm"}
ou \code{"xgboost"}.}

\item{tarefa}{\code{"auto"}, \code{"regressao"} ou \code{"classificacao"}.}

\item{seed}{Semente local; o estado aleatório da sessão é preservado.}

\item{max_depth}{Profundidade máxima das árvores (CART, forest e XGBoost).}

\item{min_n}{Mínimo por folha no CART; tamanho mínimo do nó em FIGS e ranger.}

\item{max_splits}{Orçamento total de divisões do FIGS.}

\item{trees}{Número de árvores da floresta.}

\item{mtry}{Preditores candidatos por divisão; zero usa raiz de p.}

\item{cost}{Penalidade dos erros da SVM, estritamente positiva.}

\item{gamma}{Escala do kernel da SVM.}

\item{kernel}{Kernel linear, radial, polynomial ou sigmoid.}

\item{nrounds}{Rodadas do XGBoost.}

\item{eta}{Taxa de aprendizado do XGBoost.}

\item{cp}{Parâmetro de complexidade do CART: a árvore cresce só com divisões
que melhoram o ajuste relativo em pelo menos \code{cp}; zero cresce a árvore
máxima permitida por \code{max_depth} e \code{min_n}, depois podada.}

\item{poda}{Poda do CART por custo-complexidade, escolhida pela validação
cruzada de 10 folds do \code{rpart} (Breiman et al. 1984): \code{"1ep"} fica com a
menor árvore cujo erro de validação não passa do mínimo mais um
erro-padrão; \code{"minimo"}, com a de menor erro; \code{"nenhuma"} não poda.}

\item{corte}{Na logística binária, a classe prevista é a segunda quando sua
probabilidade é maior ou igual a \code{corte} (entre 0 e 1, exclusivos).
Ignorado pelos demais modelos.}
}
\value{
Objeto \code{tr_ml_fit}.
}
\description{
Ajusta regressão linear/logística, CART, FIGS, floresta aleatória, SVM ou
XGBoost sobre preditores numéricos. Os engines além de \code{stats} são opcionais
e a mensagem de erro informa exatamente qual pacote instalar.
}
